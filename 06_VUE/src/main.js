// import './assets/main.css'

import { createApp } from 'vue'
// import App from './App.vue'
// import App from './App2.vue'
import App from './App4.vue'

// import CheckBoxItem from './components/CheckBoxItem.vue' // 전역 컴포넌트로 사용할 때만 import

// createApp(App).component('CheckBoxItem', CheckBoxItem).mount('#app') // 전역 컴포넌트 사용 시 등록
createApp(App).mount('#app')
